# 显示接口信号规格 & 计算公式汇总

> 本文档依据 `显示接口信号计算器.html` 中的实际代码规则整理,涵盖 7 种信号类型(eDP / MIPI D-PHY / MIPI C-PHY / LVDS / V-by-One / TTL / HDMI)的输入参数、计算公式、规格阈值与合规判定逻辑。
>
> 配套工具:`显示接口信号计算器.html`(单文件、离线、零依赖)。作者:AomeNero · <https://github.com/AomeNero/DSI-Calculation>

---

## 一、信号类型

下拉框顺序与可选值:

| 顺序 | 信号类型 | 内部 key | 说明 |
|:---:|---|---|---|
| 1 | **eDP** | `dp` | DisplayPort 1.4 / 2.0 双公式 + 挡位适配 |
| 2 | **MIPI D-PHY** | `mipi` | 4 组公式(按速率档位 × 时钟模式),DSI 速率 |
| 3 | **MIPI C-PHY** | `cphy` | 像素时钟 + DSI 符号率(sps) |
| 4 | **LVDS** | `lvds` | 像素时钟,按通道数(Link 数)判定 |
| 5 | **V-by-One** | `vbyone` | 按"分辨率"计算(非总时序),固定 30bit |
| 6 | **TTL** | `ttl` | 时钟频率 = 传输速率(单边沿) |
| 7 | **HDMI** | `hdmi` | HDMI 1.4 / 2.0 TMDS时钟与带宽(固定 10bit) |

---

## 二、通用参数与单位

### 单位换算

| 量 | 单位 | 换算 |
|---|---|---|
| 时序 / 分辨率 | Pixels(px) | — |
| 刷新率 | Hz | — |
| 色深 | bit | 8 / 10 / 12 |
| 时钟频率 / 像素时钟 | Hz → MHz | 1 MHz = 10⁶ Hz |
| 传输速率(D-PHY/eDP/V-by-One) | bps → Mbps → Gbps | 1 Gbps = 1000 Mbps = 10⁹ bps |
| 符号率(C-PHY DSI) | sps → Msps → Gsps | 1 Gsps = 1000 Msps = 10⁹ sps |
| C-PHY 编码 | — | 每符号 ≈ 2.28 bit(=16/7),公式中以 `/2.28` 体现 |

### Timing 设置面板(可折叠,位于页面顶部)

默认折叠,展开后可通过子参数自动计算总时序:

| 字段 | 单位 | 说明 |
|---|---|---|
| 总时序 [Total] | px | **自动计算,不可编辑** |
| 有效分辨率 [Active] | px | 水平/垂直有效像素数 |
| 后肩 [Back Porch] | px | 水平/垂直后肩 |
| 前肩 [Front Porch] | px | 水平/垂直前肩 |
| 同步宽度 [Sync Width] | px | 水平/垂直同步宽度 |

**计算公式**:
```
H_TOTAL = H_ACTIVE + H_BP + H_FP + H_SW
V_TOTAL = V_ACTIVE + V_BP + V_FP + V_SW
```

> - 水平 / 垂直各自的 4 个子参数需**全部填写有效值**才会计算对应 Total;未填全时 Total 显示为空,且不覆盖主面板的总时序值。
> - 展开 Timing 面板时,"面板参数"中的水平/垂直总时序自动变为只读;折叠后恢复可编辑。
> - 输入框支持回车键跳转到下一个(顺序:水平有效 → 垂直有效 → 水平后肩 → 垂直后肩 → 水平前肩 → 垂直前肩 → 水平同步 → 垂直同步)。
> - 面板折叠/展开状态通过 localStorage 记忆(键 `dsi-calc-timing-open`),下次打开自动恢复。

### 共享输入面板(5 个字段,始终显示)

| 字段 | 单位 | 控件 | 默认值 | 说明 |
|---|---|---|---|---|
| 水平总时序 | px | 数字 | 2560 | 正数(可由 Timing 面板自动计算) |
| 垂直总时序 | px | 数字 | 1600 | 正数(可由 Timing 面板自动计算) |
| 刷新率 | Hz | 数字 | 60 | 正数 |
| 色深 | bit | 下拉 | 8 | 8 / 10 / 12 |
| Lane 数 | — | 下拉 | 1 | **1 / 2 / 4 / 8 / 16 / 32** |

> **字段联动**
> - 选择 **LVDS** 时,"Lane 数"下拉切换为 **Link 数**(单 / 双 / 四 / 八通道),色深不用。
> - 选择 **TTL** 时,整个 Lane/Link 字段**隐藏**,色深也不用。
> - 选择 **V-by-One** 时,额外显示 **水平分辨率 / 垂直分辨率**(3840 / 2160),且**不使用**总时序与色深。
> - 选择 **HDMI** 时,固定 10bit、Symbol per clock=1,不使用色深与 Lane。

---

## 三、eDP(对应内部 `dp`)

**使用参数**:水平总时序 · 垂直总时序 · 刷新率 · 色深 · Lane

### 计算公式(两套,同时显示)

| 版本 | 系数 | 公式 |
|---|:---:|---|
| **DP 1.4** | ×1.25 | `DSI(bps) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz) × (色深(bit) × 3) × 1.25 / Lane` |
| **DP 2.0** | ×1.05 | `DSI(bps) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz) × (色深(bit) × 3) × 1.05 / Lane` |

> 结果同时显示 **DP 1.4 速率** 与 **DP 2.0 速率**(均为 Gbps/Lane)。

### 挡位适配(列表形式,与 MIPI D-PHY 风格一致)

对计算出的速率,在以下挡位集合中取**不小于速率的最小挡位**作为适配结果;若无任何挡位 ≥ 速率,则标"超规"(红色)。

| 接口模式 | 支持挡位(Gbps) | 判定依据速率 | 显示条件 |
|---|---|---|---|
| **DP(DP 1.4)** | 1.62 / 2.7 / 5.4 / 8.1 | DP 1.4 速率 | 始终显示 |
| **ALPDP(DP 1.4)** | 1.62 / 2.16 / 2.7 / 3.24 / 3.78 / 4.32 / 5.4 / 8.1 | DP 1.4 速率 | 始终显示 |
| **ALPDP(DP 2.0)** | 8.1 / 10 / 13.5 / 20 | **DP 2.0 速率** | **仅当 DP 1.4 速率 > 8.1 Gbps 时才显示** |

**判定规则**:DP(DP 1.4) 与 ALPDP(DP 1.4) 按 **DP 1.4 速率** 匹配;ALPDP(DP 2.0) 按 **DP 2.0 速率** 匹配。

**举例**
- DP 1.4 速率 = 12.39 Gbps(→ DP 2.0 速率 ≈ 10.41 Gbps):
  - DP(DP 1.4):**超规**(>8.1)
  - ALPDP(DP 1.4):**超规**(>8.1)
  - ALPDP(DP 2.0):**13.5 Gbps 适用**(因 12.39 > 8.1,显示该行;按 10.41 匹配)
- DP 1.4 速率 = 3.39 Gbps:
  - DP(DP 1.4):**5.4 Gbps 适用**
  - ALPDP(DP 1.4):**3.78 Gbps 适用**
  - ALPDP(DP 2.0):不显示(≤8.1)

---

## 四、MIPI D-PHY(对应内部 `mipi`)

**使用参数**:水平总时序 · 垂直总时序 · 刷新率 · 色深 · Lane

### 计算公式(4 组)

```
DSI(bps) = (水平总时序(Pixels) + 加数) × 垂直总时序(Pixels) × 刷新率(Hz) × 色深(bit) × 3 / Lane
```

按 **目标速率档位** 与 **时钟模式** 选择不同加数(协议开销):

| 速率档位 | 时钟模式 | 水平时序加数 |
|---|---|:---:|
| < 1.5 Gbps | 非离散(连续时钟) | **+95** |
| < 1.5 Gbps | 离散(Burst/突发) | **+190** |
| > 1.5 Gbps | 非离散 | **+145** |
| > 1.5 Gbps | 离散 | **+255** |

### 结果表与自洽高亮

4 组公式全部计算并列出;每行判定**自洽性**(公式档位与计算结果是否一致),自洽行高亮"适用":
- `<1.5 Gbps` 档的行:计算结果 **< 1.5 Gbps** → 自洽;
- `>1.5 Gbps` 档的行:计算结果 **≥ 1.5 Gbps** → 自洽。

### DSI 上限

- **上限:2.5 Gbps/Lane**(>1.5 Gbps 档的上限为 2.5 G)。
- 取自洽行中**最大速率**,若 > 2.5 Gbps → 提示(红色 14px):`DSI 速率 X.XX Gbps 超出上限 2.5 Gbps (>1.5Gbps )`;
- 否则提示:`DSI 速率在规格内(上限 2.5 Gbps/Lane)`。

---

## 五、MIPI C-PHY(对应内部 `cphy`)

**使用参数**:水平总时序 · 垂直总时序 · 刷新率 · 色深 · Lane

### 计算公式

```
像素时钟(MHz) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz) ÷ 10⁶
DSI 速率(sps) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz) × 3 × 色深(bit) × 1.2 / (Lane × 2.28)
```

> 固定常数:`×3`、`×1.2`、`/2.28`(2.28 ≈ 每符号 16/7 bit)。色深取自共享下拉(默认 8 bit)。

### 像素时钟规格(361 平台 / 2832 芯片)

单 TTL 像素时钟上限 **160 MHz**;1 个 2832 = 2 个 TTL;2 个 2832 = 4 个 TTL = 平台上限。

| 像素时钟 | 判定 | 建议配置 |
|---|---|---|
| ≤ 320 MHz | ✅ 在规格内 | **1 个 2832、2 个 TTL**(上限 320 MHz) |
| 320 < ≤ 640 MHz | ✅ 在规格内 | **2 个 2832、4 个 TTL**(上限 640 MHz) |
| > 640 MHz | ⚠️ 超规 | 超出 361 平台规格上限(640 MHz) |

### DSI 符号率上限

- **上限:1.5 Gsps/Lane**(符号率,sps)。
- DSI 速率 > 1.5 Gsps → 超规;否则在规格内。

---

## 六、LVDS(对应内部 `lvds`)

**使用参数**:水平总时序 · 垂直总时序 · 刷新率 · Link 数(**不使用色深**)

> 选 LVDS 时,Lane 数下拉切换为 **Link 数**:单通道(1)/ 双通道(2)/ 四通道(4)/ 八通道(8)。

### 计算公式

```
像素时钟(MHz) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz) ÷ 10⁶
Single-link LVDS像素时钟 PCLK(MHz) = 像素时钟(MHz) ÷ Link 数
```

### 显示

- **大字**:`Single-link LVDS 像素时钟(PCLK)` = 像素时钟 ÷ Link 数
- **小字**:`像素时钟`(总)= 水平总时序 × 垂直总时序 × 刷新率 ÷ 10⁶

### 规格阈值(@0.1 MHz 步进)

按所选 Link 数对应的通道规格(以**总像素时钟**判定):

| Link 数 | 通道 | 像素时钟范围 | 等效(单 Link 基准 10–135 MHz) |
|:---:|---|---|---|
| 1 | 单通道 Single | **10 ~ 135 MHz** | 10 ~ 135 MHz |
| 2 | 双通道 Dual | **20 ~ 270 MHz** | 2 × (10 ~ 135) |
| 4 | 四通道 Quad | **40 ~ 540 MHz** | 4 × (10 ~ 135) |
| 8 | 八通道 Octal | **80 ~ 1080 MHz** | 8 × (10 ~ 135) |

### 判定规则

- 总像素时钟 **> 上限** → ⚠️ `超出{通道}上限(XXX MHz),建议增加通道数`
- 总像素时钟 **< 下限** → ⚠️ `低于{通道}下限(XX MHz),可减少通道数`
- 总像素时钟 **在范围内** → ✅ `在{通道}规格内(XX–XXX MHz @0.1MHz 步进)`

---

## 七、V-by-One(对应内部 `vbyone`)

**使用参数**:水平分辨率 · 垂直分辨率 · 刷新率 · Lane(**不使用总时序 / 色深**;严格按文档:固定 30bit、保留 ×1.2)

### 计算公式

```
每 Lane 数据速率(bps) = 水平分辨率(Pixels) × 垂直分辨率(Pixels) × 1.2 × 30(bit) × 刷新率(Hz) / Lane
```

> `1.2` = 消隐系数(20% 消隐区);`30(bit)` = 10bit RGB 固定(10×3)。

### 规格阈值

- **上限:3.75 Gbps/Lane**(目前最高数据速率)。
- 每 Lane 速率 ≤ 3.75 Gbps → ✅ 在规格内;> 3.75 Gbps → ⚠️ 超规格。

> ⚠️ 备注:源文档《显示接口信号计算公式汇总.md》中 V-by-One 举例(746.5 Mbps)存在 ×10 笔误;本工具按公式 `×30` 正确计算(如 3840×2160×1.2×30×60÷8 ≈ **2.24 Gbps/Lane**)。

---

## 八、TTL(对应内部 `ttl`)

**使用参数**:水平总时序 · 垂直总时序 · 刷新率(**不使用色深 / Lane**,Lane 字段隐藏)

### 计算公式

```
时钟频率(Hz) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz)   (单边沿采样,即传输速率)
```

> 采用单边沿采样,一个时钟周期传送 1 bit,故**时钟频率即对应的传输速率**(MHz = Mbps)。

### 规格阈值

- **上限:100 Mbps**(规格书标定最高)。
- 时钟频率 ≤ 100 Mbps → ✅ 在规格内;> 100 Mbps → ⚠️ 超规格。

---

## 八(续)、HDMI(对应内部 `hdmi`)

**使用参数**:水平总时序 · 垂直总时序 · 刷新率(**固定 10bit,Symbol per clock=1,不使用色深 / Lane**)

### 固定参数

| 参数 | 值 | 说明 |
|---|---|---|
| 色深 | 10 bit | 固定 |
| Color Depth Factor | 1.25 | 固定 |
| Symbol per clock | 1 | 固定 |

### 计算公式

```
像素时钟(MHz) = 水平总时序(Pixels) × 垂直总时序(Pixels) × 刷新率(Hz) ÷ 10⁶

HDMI 1.4: TMDS时钟(MHz) = 像素时钟 × 1 × 1.25
HDMI 2.0: TMDS时钟(MHz) = 像素时钟 × 1 × 1.25 ÷ 4

TMDS带宽(Gbps) = TMDS时钟(MHz) × 10(固定10bit) ÷ 1000
```

### 结果展示

- 像素时钟单独显示(大字)
- HDMI 1.4 / 2.0 对比表格:显示各自的 TMDS时钟与 TMDS带宽
- 表格中标注合规/超规状态
- 底部给出总体规格判定

### 总体判定规则

| HDMI 1.4 | HDMI 2.0 | 判定 |
|:---:|:---:|---|
| 超规 | 超规 | ⚠️ `HDMI 1.4 和 2.0 TMDS带宽均超出规格(> 18 Gbps)` |
| 超规 | 合规 | ⚠️ `HDMI 1.4 TMDS带宽超出规格,建议使用 HDMI 2.0` |
| 合规 | 超规 | ✅ `HDMI 1.4 合规,HDMI 2.0 超规但可通过降频使用` |
| 合规 | 合规 | ✅ `HDMI 1.4 和 2.0 TMDS带宽均在规格内(≤ 18 Gbps)` |

### 规格阈值

- **TMDS带宽上限:18 Gbps**
- HDMI 1.4 TMDS带宽 ≤ 18 Gbps → ✅ 合规;> 18 Gbps → ⚠️ 超规
- HDMI 2.0 TMDS带宽 ≤ 18 Gbps → ✅ 合规;> 18 Gbps → ⚠️ 超规

---

## 九、交互与校验规则

### 计算
- **实时计算**:任一输入变化立即重算当前选中信号的结果。
- **"计算"按钮**:强制校验全部所需字段(标记 touched)并重算,结果区附反馈动效。
- **状态保留**:各输入值保留在内存中,切换信号类型再切回不丢失;输入值不写 localStorage(仅 Timing 面板折叠状态经 localStorage 记忆)。

### 字段校验
| 字段 | 规则 |
|---|---|
| 水平/垂直总时序、水平/垂直分辨率、刷新率 | 非空 · 数字 · > 0 |
| 色深 | 下拉枚举(8/10/12),无需校验 |
| Lane 数 | 下拉枚举(1/2/4/8/16/32),恒为合法 |
| Link 数(LVDS) | 下拉枚举(1/2/4/8),恒为合法 |

- 非法字段:输入框下方红字提示(如"请输入数值""需大于 0"),结果区显示"请先修正高亮的输入项",**不输出错误计算结果**。
- 仅当前信号**所需**字段参与校验(如 LVDS/TTL 不校验色深与 Lane)。

### 提示样式(verdict)
| 级别 | 含义 | 样式 |
|---|---|---|
| `ok` | 在规格内 | 绿底 + ✓ 图标 |
| `warn` | 超规格 / 超上限 | 琥珀底 + ⚠ 图标,**文字红色、字号 14px**(所有信号类型统一) |
| `info` | 说明性提示 | 灰底 |

### 计算值高亮
- 大字号结果数值(及 MIPI 表格中的速率值)使用蓝色 `#1d4ed8`;单位后缀(MHz / Gbps 等)为灰色,形成主-次层级。

---

## 十、公式速查表

| 信号 | 核心公式 | 关键输出 | 规格上限 |
|---|---|---|---|
| **eDP** | `H×V×刷新率×(色深×3)×{1.25 \| 1.05}/Lane` | DP 1.4 / DP 2.0 速率(Gbps/Lane)+ 挡位适配 | DP 1.4 挡位 1.62/2.7/5.4/8.1;ALPDP 1.4 含 2.16/3.24/3.78/4.32;ALPDP 2.0 = 8.1/10/13.5/20 |
| **MIPI D-PHY** | `(H+加数)×V×刷新率×色深×3/Lane` | DSI 速率(Mbps/Gbps per Lane) | DSI ≤ **2.5 Gbps/Lane** |
| **MIPI C-PHY** | 像素时钟=`H×V×刷新率÷10⁶`;DSI=`H×V×刷新率×3×色深×1.2/(Lane×2.28)` | 像素时钟(MHz)+ DSI(sps) | 像素时钟 ≤640 MHz(平台);DSI ≤ **1.5 Gsps/Lane** |
| **LVDS** | 像素时钟=`H×V×刷新率÷10⁶`;PCLK=像素时钟÷Link数 | Single-link PCLK + 总像素时钟(MHz) | 单/双/四/八通道:10–135 / 20–270 / 40–540 / 80–1080 MHz |
| **V-by-One** | `水平分辨率×垂直分辨率×1.2×30×刷新率/Lane` | 每 Lane 数据速率(Gbps/Lane) | ≤ **3.75 Gbps/Lane** |
| **TTL** | `H×V×刷新率`(=传输速率) | 时钟频率 / 速率(MHz/Mbps) | ≤ **100 Mbps** |
| **HDMI** | 像素时钟=`H×V×刷新率÷10⁶`;TMDS=`像素时钟×1.25`(1.4)/`像素时钟×1.25÷4`(2.0) | TMDS时钟(MHz)+ TMDS带宽(Gbps) | TMDS带宽 ≤ **18 Gbps** |

---

*本文档与 `显示接口信号计算器.html` 实现保持一致;若代码规则更新,本文档应同步修订。*
